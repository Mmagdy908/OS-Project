#include "mmu.h"
#include <stdio.h>

int setup_page_table(int pid)
{
    // Temporary stub — does nothing
    printf("[MMU] setup_page_table called for PID %d (stub)\n", pid);
    return -1; // temp value 
}
